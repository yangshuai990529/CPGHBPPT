# TCL Product Template
Runtime copy of the repository-root `PPT设计规范/产品PPT模板.pptx`; the source template remains authoritative and untouched. `./install.sh` creates this ignored copy automatically. Replacing the source master requires design-token and layout compatibility checks; arbitrary templates are not automatically supported by the current renderer.
