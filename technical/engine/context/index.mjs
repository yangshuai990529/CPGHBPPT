export function reasoningContext(dataset,role='competitor-analysis') {const select=role==='competitor-analysis'?dataset.evidence:role==='market-analysis'?dataset.evidence.filter(e=>e.capability==='Market Data'):dataset.evidence.filter(e=>e.confidence==='high'); return {role,evidence:select.map(e=>({evidence_id:e.evidence_id,claim_id:e.claim_id,quote_or_fact:e.quote_or_fact,entity:e.entity,capability:e.capability,source_id:e.source_id})),sources:dataset.sources.filter(s=>select.some(e=>e.source_id===s.source_id)).map(s=>({source_id:s.source_id,title:s.title,url:s.locator.uri,authority_level:s.authority_level})),gaps:dataset.coverage.rows.flatMap(r=>r.gaps)}; }
