import fs from 'node:fs/promises';import path from 'node:path';
export async function workflow(root,config){const registry=JSON.parse(await fs.readFile(path.join(root,'presentation-types/registry.json'),'utf8'));const type=registry.types[config.presentation.type];if(!type)throw new Error('未知演示类型');return {type:config.presentation.type,definition:type,stages:['UNDERSTANDING','KNOWLEDGE_RETRIEVAL','PLANNING',...((config.research.enabled && config.research.source_mode !== "provided-only")?['RESEARCHING','VISUAL_RESEARCH']:[]),...((config.reasoning.enabled && config.research.enabled && config.research.source_mode !== "provided-only")?['ANALYZING']:[]),'STORYLINING',...(config.output.pptx?['RENDERING','QA']:[])],skipped:[...(!(config.research.enabled && config.research.source_mode !== "provided-only")?['RESEARCHING','VISUAL_RESEARCH']:[]),...(!config.reasoning.enabled?['ANALYZING']:[]),...(!config.output.pptx?['RENDERING','QA']:[])]};}
