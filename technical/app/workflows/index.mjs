import fs from 'node:fs/promises';import path from 'node:path';
export async function workflow(root,config){const registry=JSON.parse(await fs.readFile(path.join(root,'presentation-types/registry.json'),'utf8'));const type=registry.types[config.presentation.type];if(!type)throw new Error('未知演示类型');const online=config.research.enabled&&['web','hybrid','public'].includes(config.research.source_mode),visual=online&&config.visual?.enabled!==false;return {type:config.presentation.type,definition:type,stages:['UNDERSTANDING','KNOWLEDGE_RETRIEVAL','PLANNING',...(online?['RESEARCHING']:[]),...(visual?['VISUAL_RESEARCH']:[]),...((config.reasoning.enabled&&online)?['ANALYZING']:[]),'STORYLINING','VISUAL_PLANNING','ASSET_PROCESSING','LAYOUT_PLANNING',...(config.output.pptx?['RENDERING','QA']:[])],skipped:[...(!online?['RESEARCHING']:[]),...(!visual?['VISUAL_RESEARCH']:[]),...(!config.reasoning.enabled?['ANALYZING']:[]),...(!config.output.pptx?['RENDERING','QA']:[])]};}
