# Picture Quality — candidate method context

Before comparing vendors, fix the model, market, firmware, signal type, picture mode and observation/test conditions. Separate marketed feature presence from measured quality and user-perceived effect. Use only the dimensions required to answer the brief; unobserved is `unknown`, not `unsupported`.
