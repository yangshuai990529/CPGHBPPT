import fs from 'node:fs/promises';import path from 'node:path';import {fileURLToPath} from 'node:url';import {runResearch} from '../research/index.mjs';import {makeEvidenceDeck} from '../reasoning/evidence-deck.mjs';
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'../..');const brief=JSON.parse(await fs.readFile(path.join(root,'tests/ai-picture/research-brief.json'),'utf8'));const out=path.join(root,'output/AI-Picture-Research');const d=await runResearch({brief,outputDir:out,cacheDir:path.join(root,'cache/research')});
if(d.sources.length<3||d.evidence.length<3)throw new Error('Evidence gate failed: three official sources required');const result=await makeEvidenceDeck(d,root,out);if(result.insight.status!=='supported')throw new Error('Insight gate failed');console.log(`Research → Reasoning → Storyline → Slide Spec complete: ${out}`);
