export function summarize(dataset){const facts=dataset.claims.filter(c=>c.status==='SUPPORTED');return ['# Research Summary (facts only)',`Fetched ${dataset.sources.length} sources. ${facts.length} supported literal claims; ${dataset.claims.length-facts.length} unsupported/low-confidence.`, '## FACTS',...facts.map(c=>`- ${c.statement} (${c.evidence_ids.join(', ')})`),'## GAPS',...dataset.coverage.rows.flatMap(r=>r.gaps.map(g=>`- ${g}: no reliable evidence found, not a negative claim.`)),'## CONFLICTS',...dataset.conflicts.map(c=>`- ${c.key}: ${c.claim_ids.join(', ')}`),'## UNSUPPORTED CLAIMS',...dataset.claims.filter(c=>c.status==='UNSUPPORTED').map(c=>`- ${c.statement}`),'## NEXT ANALYSIS QUESTIONS','- Which common patterns are supported across brands? (Product Reasoning only)','- What internal TCL evidence and user research are needed before opportunities or strategy?'].join('\n')+'\n';}
