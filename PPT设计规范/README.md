# PPT 设计规范

`产品PPT模板.pptx` 是 CPGHBPPT 当前验证的设计模板。`./install.sh` 或 `./technical/setup.sh` 会把它复制到运行时位置 `technical/templates/tcl-product/master.pptx`；运行时副本不提交到 Git。

此模板与其中的品牌元素不在项目 MIT 源码许可范围内。仓库所有者已明确授权将其发布到本仓库，但使用者仍需自行确认具体场景的使用权限。
